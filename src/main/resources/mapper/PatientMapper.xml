<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.healthware.mapper.PatientMapper">

    <select id="selectByUserId" resultType="com.healthware.vo.PatientVO">
        SELECT id, user_id AS userId, name, gender, age, id_card AS idCard,
               phone, address, medical_history AS medicalHistory,
               allergy_history AS allergyHistory, relationship, create_time AS createTime
        FROM patient
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

</mapper>
