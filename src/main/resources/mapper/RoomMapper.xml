<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.healthware.mapper.RoomMapper">

    <select id="selectByDepartmentId" resultType="com.healthware.vo.RoomVO">
        SELECT r.id, r.name, r.department_id AS departmentId, dept.name AS departmentName,
               r.location, r.capacity, r.status
        FROM room r
        LEFT JOIN department dept ON dept.id = r.department_id
        WHERE r.department_id = #{deptId} AND r.status = 1
    </select>

</mapper>
